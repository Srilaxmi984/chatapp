## Packages
date-fns | Date formatting for chat timestamps
framer-motion | Smooth animations for messages and transitions

## Notes
WebSocket connection required at protocol://host/ws
Authentication uses Replit Auth (useAuth hook already provided)
